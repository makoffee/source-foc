<footer class="gh-footer{{#match @custom.header_and_footer_color "Accent color"}} has-accent-color{{/match}} gh-outer">
    <div class="gh-footer-inner gh-inner">

        <div class="gh-footer-bar">
            <div class="gh-footer-row-top">
                <nav class="gh-footer-menu">
                    {{navigation type="secondary"}}
                </nav>

                <div class="gh-footer-social">
                    {{#if @site.facebook}}
                        <a href="{{social_url type="facebook"}}" target="_blank" rel="noopener" aria-label="Facebook">
                            <img src="{{asset "images/facebook.svg"}}" alt="Facebook" width="38" height="38">
                        </a>
                    {{/if}}

                    {{#if @site.twitter}}
                        <a href="{{social_url type="twitter"}}" target="_blank" rel="noopener" aria-label="Instagram">
                            <img src="{{asset "images/instagram.svg"}}" alt="Instagram" width="38" height="38">
                        </a>
                    {{/if}}

                    {{#if @custom.linkedin_url}}
                        <a href="{{@custom.linkedin_url}}" target="_blank" rel="noopener" aria-label="LinkedIn">
                            <img src="{{asset "images/linkedin.svg"}}" alt="LinkedIn" width="38" height="38">
                        </a>
                    {{/if}}
                </div>
            </div>

            <div class="gh-footer-row-bottom">
                <div class="gh-footer-copyright">
                    &copy; <script>document.write(new Date().getFullYear());</script> {{@site.title}}
                </div>
            </div>
        </div>

        {{#if @site.members_enabled}}
            {{#unless @member}}
                <section class="gh-footer-signup">
                    <h2 class="gh-footer-signup-header is-title">
                        {{#if @custom.signup_heading}}{{@custom.signup_heading}}{{else}}{{@site.title}}{{/if}}
                    </h2>
                    <p class="gh-footer-signup-subhead is-body">
                        {{#if @custom.signup_subheading}}{{@custom.signup_subheading}}{{else}}{{@site.description}}{{/if}}
                    </p>
                    {{> "email-subscription" email_field_id="footer-email"}}
                </section>
            {{/unless}}
        {{/if}}

    </div>
</footer>